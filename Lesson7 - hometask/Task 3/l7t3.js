function doFunction(x, y, doMath) {
	return doMath(x, y);
}

function power(x, y) {
	return Math.pow(x, y);
}

function sum(x, y) {
	return x + y;
}

function div(x, y) {
	return x / y;
}

function mul(x, y) {
	return x * y;
}
